
  <h2 class="country-name">{{name}}</h2>
  <img src="{{flag}}" alt="{{name}}" width="400" height="250">
  <div class="country-info">
    <p class="capital">Capital: {{capital}}</p>
    <p class="population">Population: {{population}}</p>
    <p class="langs">Languages: {{name}}</p>    
  </div>
